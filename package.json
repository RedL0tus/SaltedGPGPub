{
  "private": true,
  "name": "salted_gpg_pub",
  "version": "0.1.0",
  "description": "A simple CloudFlare Workers site for publishing GPG/OpenPGP public keys",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Kay Lin <i@v2bv.net>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  }
}
